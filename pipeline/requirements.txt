# Runtime dependencies for the extraction, validation and evaluation pipeline.
# Derived from the imports actually present in the tracked scripts.

# --- LLM extraction -----------------------------------------------------
# extract_career_graph.py, batch_extract_career_graphs.py, enrich_career_graphs.py
anthropic>=0.40
python-dotenv>=1.0
tqdm>=4.66

# Optional extraction backends. extract_career_graph.py and
# batch_extract_career_graphs.py import these at module level, so they are
# required to run those scripts even when only Claude is used.
google-generativeai>=0.8
openai>=1.40
ollama>=0.3

# --- Scraping -----------------------------------------------------------
# batch_scrape_new_edcs.py
pandas>=2.0
requests>=2.31

# --- Evaluation ---------------------------------------------------------
# evaluate_against_edh.py, fetch_tm_persons.py, evaluate_name_components.py
# use requests (above); the remaining evaluation scripts use only the
# standard library (csv, json, argparse, pathlib, collections).

# --- Vector search (optional) -------------------------------------------
# upsert_to_pinecone.py imports these lazily inside functions.
pinecone>=5.0
rdflib>=7.0
